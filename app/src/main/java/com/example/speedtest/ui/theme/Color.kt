package com.example.speedtest.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * ═══════════════════════════════════════════════════════════════
 *  Color.kt — Palet warna Pink & Cute untuk Speed Test App
 * ═══════════════════════════════════════════════════════════════
 */

// ── Primary: Pink Palette ────────────────────────────────────
val PinkPrimary = Color(0xFFFF69B4) // Hot Pink
val PinkPrimaryDark = Color(0xFFC71585) // Medium Violet Red
val PinkLight = Color(0xFFFFB6C1) // Light Pink

// ── Secondary: Rose/Purple ───────────────────────────────────
val RoseSecondary = Color(0xFFF06292)
val RoseLight = Color(0xFFF8BBD0)
val RoseDark = Color(0xFF880E4F)

// ── Gauge Gradient Colors (Pink to Purple) ──────────────────
val GaugeLow = Color(0xFFFFB6C1)       // Light Pink
val GaugeMedium = Color(0xFFFF69B4)    // Hot Pink
val GaugeHigh = Color(0xFFDA70D6)      // Orchid
val GaugeMax = Color(0xFF9932CC)       // Dark Orchid

// ── Background & Surface (Soft Pinkish) ──────────────────────
val PinkDarkBackground = Color(0xFF2D1B22) // Dark Plum
val PinkDarkSurface = Color(0xFF3D242D)
val PinkDarkSurfaceVariant = Color(0xFF5D3845)

val PinkLightBackground = Color(0xFFFFF0F5) // Lavender Blush
val PinkLightSurface = Color(0xFFFFFFFF)
val PinkLightSurfaceVariant = Color(0xFFFCE4EC)

// ── Text ─────────────────────────────────────────────────────
val TextOnDark = Color(0xFFFDEEF4)
val TextOnDarkSecondary = Color(0xFFD291BC)
val TextOnLight = Color(0xFF4A148C) // Deep Purple for better contrast on pink
val TextOnLightSecondary = Color(0xFF880E4F)

// ── Status Colors ────────────────────────────────────────────
val PingColor = Color(0xFFFF80AB)      // Pink A100
val DownloadColor = Color(0xFFF06292)  // Pink 300
val UploadColor = Color(0xFFBA68C8)    // Purple 300
